# NarrativeSite Website Template

This is the starter target for the Code Generation Skill.

The build agent should:

- Store user-facing content in `content/site.ts`.
- Add reusable components under `components/`.
- Keep private context out of generated source.
- Preserve semantic HTML, responsive layout, and reduced-motion behavior.
