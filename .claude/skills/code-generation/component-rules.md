# Component Rules

- Components should accept typed data props.
- Avoid hardcoded private user facts in components.
- No layout shift on hover.
- Buttons and links need accessible names.
- Use section ids that match navigation.
- Keep motion optional and disabled for reduced motion.
