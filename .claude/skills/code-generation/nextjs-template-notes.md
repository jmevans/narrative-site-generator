# Next.js Template Notes

- Use App Router under `app/`.
- Keep generated content in `content/site.ts` or JSON.
- Use reusable sections under `components/sections`.
- Use `next/image` when assets are local or remote domains are configured.
- Keep animation in small wrapper components where possible.
- Include metadata, semantic landmarks, and accessible CTAs.
